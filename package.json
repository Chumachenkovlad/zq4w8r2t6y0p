{
  "name": "training-hub",
  "version": "1.0.0",
  "description": "Персональний хаб тренувань — статичний сайт на Eleventy. Контент у Markdown, HTML будується з шаблонів.",
  "private": true,
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "clean": "rm -rf _site",
    "test": "node --test"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "js-yaml": "^4.1.0"
  }
}
